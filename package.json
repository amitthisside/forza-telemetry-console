{
  "name": "forza-telemetry-console",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo for Forza Telemetry Console",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "format": "pnpm -r format"
  }
}
