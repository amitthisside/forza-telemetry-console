apiVersion: apps/v1
kind: Deployment
metadata:
  name: telemetry-stream
  namespace: forza-telemetry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: telemetry-stream
  template:
    metadata:
      labels:
        app: telemetry-stream
    spec:
      containers:
        - name: telemetry-stream
          image: ghcr.io/forza-telemetry-console/telemetry-stream:latest
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: telemetry-stream
  namespace: forza-telemetry
spec:
  selector:
    app: telemetry-stream
  ports:
    - name: http
      port: 80
      targetPort: 8000
