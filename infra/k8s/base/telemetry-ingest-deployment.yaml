apiVersion: apps/v1
kind: Deployment
metadata:
  name: telemetry-ingest
  namespace: forza-telemetry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: telemetry-ingest
  template:
    metadata:
      labels:
        app: telemetry-ingest
    spec:
      containers:
        - name: telemetry-ingest
          image: ghcr.io/forza-telemetry-console/telemetry-ingest:latest
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: telemetry-ingest
  namespace: forza-telemetry
spec:
  selector:
    app: telemetry-ingest
  ports:
    - name: http
      port: 80
      targetPort: 8000
