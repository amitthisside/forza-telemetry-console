apiVersion: apps/v1
kind: Deployment
metadata:
  name: device-gateway
  namespace: forza-telemetry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: device-gateway
  template:
    metadata:
      labels:
        app: device-gateway
    spec:
      containers:
        - name: device-gateway
          image: ghcr.io/forza-telemetry-console/device-gateway:latest
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: device-gateway
  namespace: forza-telemetry
spec:
  selector:
    app: device-gateway
  ports:
    - name: http
      port: 80
      targetPort: 8000
