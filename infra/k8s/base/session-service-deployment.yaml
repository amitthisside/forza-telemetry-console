apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-service
  namespace: forza-telemetry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: session-service
  template:
    metadata:
      labels:
        app: session-service
    spec:
      containers:
        - name: session-service
          image: ghcr.io/forza-telemetry-console/session-service:latest
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: session-service
  namespace: forza-telemetry
spec:
  selector:
    app: session-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
